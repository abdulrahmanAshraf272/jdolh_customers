import 'package:flutter/material.dart';

class ResProductSubscreen extends StatelessWidget {
  const ResProductSubscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('res product subscreen')),
    );
  }
}
