import 'package:flutter/material.dart';

class ResServiceSubscreen extends StatelessWidget {
  const ResServiceSubscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('res Service subscreen')),
    );
  }
}
